import { ogEndpoint } from "../lib/og";

export const GET = ogEndpoint({
  label: "// ai transformation",
  heading: "Smart leaders are stuck on AI. I get their teams unstuck.",
  subtext: "You've purchased the licenses. Nothing has changed. I take a hands-on approach to showing your team what's possible.",
});
