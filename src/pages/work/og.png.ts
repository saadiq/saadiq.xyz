import { ogEndpoint } from "../../lib/og";

export const GET = ogEndpoint({
  label: "// work",
  heading: "What shipped",
  subtext: "Production agents, discovery sprints, and embedded engagements.",
});
