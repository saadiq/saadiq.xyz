import { ogEndpoint } from "../../lib/og";

export const GET = ogEndpoint({
  label: "// services",
  heading: "How I work",
  subtext: "Talks, sprints, builds, and embedded technical leadership.",
});
